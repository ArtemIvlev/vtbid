title: PartyRef
type: object
description: Ссылка на связанные "party"
properties:
  '@referredType':
    type: string
    description: Generic attribute indicating the name of the class type of the referred resource entity.
  description:
    type: string
    description: Текст описывающий сторону на которую ссылаются (Жена, "Компания работодатель")
  href:
    type: string
    description: Ссылка (url) на связанные "party"
  id:
    type: string
    description: Уникальный идентификатор "party" на которую идет ссылка
  name:
    type: string
    description: Имя "party" (Ивлев Артем, ПАО ВТБ)
  validFor:
    $ref: ./validFor.yaml
required:
  - href
  - id
