title: contactMedium
type: object
properties:
  validFor:
    $ref: ./validFor.yaml
  preferred:
    type: string
  vaidated:
    type: boolean
  oneof:
    - email:
        $ref: ./email.yaml
  metadata:
    $ref: ./metadata.yaml