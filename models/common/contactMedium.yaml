title: contactMedium
type: object
properties:
  preferred:
    type: string
  vaidated:
    type: boolean
  mediumType:
    type: string
    enum:
      - email
      - phone
  characteristic:
    oneOf:
      - $ref: './email.yaml#'
      - $ref: ./phoneNumber.yaml
    description: Email или Номер телефона
  metadata:
    $ref: ./metadata.yaml
