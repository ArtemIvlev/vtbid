title: contactMedium
type: object
properties:
  contactId: 
    type: string
    format: uuid
  type:
    type: string
    enum: [email, phone]
  value:
    oneOf: 
      - $ref: ./email.yaml
      - $ref: ./phoneNumber.yaml
  metadata:
    $ref: ./metadata.yaml
