title: contactMedium
type: object
properties:
  validFor:
    $ref: ./validFor.yaml
  preferred:
    type: string
  vaidated:
    type: boolean
  value:
    oneOf:
      - $ref: './email.yaml'
      - $ref: ./phoneNumber.yaml
  metadata:
    $ref: ./metadata.yaml