title: contactMedium
type: object
properties:
  validFor:
    $ref: ./validFor.yaml
  preferred:
    type: string
  vaidated:
    type: boolean
  oneof:
  - $ref: ./phoneNumber.yaml
  - $ref: ./email.yaml

